# Ruben Sanduleac

class Question:
    """Default constructor that initializes the question and answers to itself."""
    def __init__(self, q_text, q_answer):
        self.text = q_text
        self.answer = q_answer
